type Query {
    products: [Product],
    product(id: ID): Product
}

type Product {
    id: ID,
    name: String,
    brand: String,
    priceTags: [PriceTag]
}

type PriceTag {
    description: String,
    value: String
}