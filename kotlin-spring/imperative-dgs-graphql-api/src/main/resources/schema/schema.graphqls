type Query {
    shows(titleFilter: String): [Show]
}

type Show {
    id: ID
    title: String
    releaseYear: Int
    ratings: [Rating]
}

type Rating {
    id: ID
    rateValue: Int
    comment: String
    showId: ID
}